{
  "name": "synctable",
  "version": "0.13.0",
  "description": "Cross-browser tree backup and workspace synchronization utility (Desktop App)",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/oscarqht/synctable-ai-studio.git",
    "directory": "apps/synctable-desktop"
  },
  "homepage": "https://github.com/oscarqht/synctable-ai-studio",
  "scripts": {
    "build:native": "bun run ./scripts/build-native.ts",
    "build:css": "tailwindcss -i ./src/mainview/tailwind.css -o ./src/mainview/style.css --minify",
    "predev": "bun run build:native && bun run build:css",
    "dev": "electrobun dev",
    "start": "electrobun dev",
    "prebuild": "bun run build:native && bun run build:css",
    "build": "electrobun build",
    "postpackage:win": "bun run ./scripts/post-package-win.ts",
    "postpackage:mac": "bun run ./scripts/post-package-mac.ts",
    "package": "bun run build:native && bun run build:css && electrobun build --env=stable && bun run postpackage:win && bun run postpackage:mac",
    "package:mac": "bun run build:native && bun run build:css && electrobun build --env=stable && bun run postpackage:mac",
    "package:win": "bun run build:native && bun run build:css && electrobun build --env=stable && bun run postpackage:win",
    "package:desktop": "bun run build:native && bun run build:css && electrobun build --env=stable && bun run postpackage:win && bun run postpackage:mac",
    "prep:npm": "bun run ./scripts/prepare-npm-package.ts",
    "pack:npm": "bun run prep:npm && npm pack",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "bin": {
    "synctable": "bin/cli.mjs",
    "synctable-desktop": "bin/cli.mjs"
  },
  "files": [
    "bin",
    "dist"
  ],
  "dependencies": {},
  "devDependencies": {
    "@synctable/ui": "*",
    "@types/bun": "^1.3.14",
    "@types/lz4js": "^0.2.2",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@types/three": "^0.185.4",
    "autoprefixer": "^10.4.19",
    "clsx": "^2.1.1",
    "electrobun": "^1.18.1",
    "lucide-react": "^0.400.0",
    "lz4js": "^0.2.0",
    "postcss": "^8.4.39",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tailwind-merge": "^2.4.0",
    "tailwindcss": "^3.4.6",
    "typescript": "^5.5.3"
  }
}
